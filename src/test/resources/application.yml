server:
  client:
    key-store: client/apigw.jks
    key-store-password: s3cr3t
    trust-store: client/apigw.jks
    trust-store-password: s3cr3t
    protocol: TLSv1.2
eureka:
  client:
    enabled: false
service:
  api:
    url: https://localhost:${server.port}/api
